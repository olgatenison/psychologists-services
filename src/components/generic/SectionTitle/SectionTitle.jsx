import React from "react";
import { SectionTitleStyled } from "./SectionTitle.styles.js";

const SectionTitle = ({ children }) => {
  return <SectionTitleStyled>{children}</SectionTitleStyled>;
};

export default SectionTitle;
